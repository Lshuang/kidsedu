# 🧒 幼小衔接多娃学习工作台 · 结构化提示词文档

> 一套经过实战验证的 AI 提示词（Prompt），用于指导 AI 编程助手从零构建"幼小衔接（5-6 岁）多子女家庭学习激励 Web App"，并完成 PWA 化与免费公网部署。
> 本文档由实际开发对话整理而成，产出物即本仓库：[在线演示](https://lshuang.github.io/kidsedu/)。

---

## 📑 目录

- [1. 标题与简介](#1-标题与简介)
  - [1.1 用途](#11-用途)
  - [1.2 适用场景](#12-适用场景)
  - [1.3 使用前提](#13-使用前提)
- [2. 核心提示词内容（按功能模块）](#2-核心提示词内容按功能模块)
  - [模块 A：总体需求与技术约束](#模块-a总体需求与技术约束)
  - [模块 B：多小孩账号系统](#模块-b多小孩账号系统)
  - [模块 C：幼小衔接知识库](#模块-c幼小衔接知识库)
  - [模块 D：智能每日计划与错题复习](#模块-d智能每日计划与错题复习)
  - [模块 E：语音朗读与音色优选](#模块-e语音朗读与音色优选)
  - [模块 F：娱乐/电视时长管理](#模块-f娱乐电视时长管理)
  - [模块 G：多元化积分商城](#模块-g多元化积分商城)
  - [模块 H：家长控制中心](#模块-h家长控制中心)
  - [模块 I：PWA 化与免费部署](#模块-ipwa-化与免费部署)
  - [模块 J：大规模内容扩容（并行子代理）](#模块-j大规模内容扩容并行子代理)
- [3. 使用示例](#3-使用示例)
  - [示例 1：一次性完整生成](#示例-1一次性完整生成)
  - [示例 2：增量添加新板块](#示例-2增量添加新板块)
  - [示例 3：部署到 GitHub Pages](#示例-3部署到-github-pages)
  - [示例 4：内容批量扩容](#示例-4内容批量扩容)
- [4. 参数说明](#4-参数说明)
- [5. 注意事项与最佳实践](#5-注意事项与最佳实践)

---

## 1. 标题与简介

### 1.1 用途

本提示词用于让 AI 编程助手（如 WorkBuddy / CodeBuddy / Claude / GPT 等）**一次性或分阶段生成**一个功能完整的儿童学习激励应用，具备以下能力：

- 多小孩账号切换、数据完全隔离（localStorage 持久化）
- 拼音 / 识字 / 古诗 / 英语 / 数学 / 科普 / 故事 七大知识库
- 习惯打卡、积分盲盒、彩带动画等正向激励
- 积分商城（非物质奖励优先）+ 娱乐计时器
- 家长控制中心（隐蔽解锁 + 教育避坑指南）
- Web Speech API 语音朗读（自动优选播音级音色）
- 可安装 PWA（离线可用、添加到手机/平板桌面）

### 1.2 适用场景

| 场景 | 说明 |
|---|---|
| 家庭自用 | 为自家孩子快速搭一套学习打卡工具，免费部署即用 |
| 二次开发 | 在本仓库基础上改知识库、换主题风格、加新板块 |
| 提示词学习 | 学习"复杂 Web App 需求如何拆解为可执行提示词" |
| 教学演示 | 演示 AI 辅助开发从需求 → 代码 → 部署的全流程 |

### 1.3 使用前提

- AI 助手需具备**文件读写与命令执行**能力（部署环节需要 git / curl）
- 无需任何后端与数据库，产出为纯静态站点
- 部署到 https 环境后才能体验 PWA 安装与离线能力

---

## 2. 核心提示词内容（按功能模块）

> 使用方式：模块 A 为必选底座；B–H 可全量拼接一次性生成，也可逐模块增量下发。
> `{占位符}` 为可自定义参数，取值见 [第 4 节](#4-参数说明)。

### 模块 A：总体需求与技术约束

**说明**：定义产品定位、目标用户、界面风格与技术边界，是所有后续模块的上下文基础。

```text
请帮我开发一个专为"幼小衔接/幼儿园大班（{age_range}）"及多子女家庭设计的
响应式 Web 交互工作台 (Web App)，必须同时完美适配手机、平板和电脑屏幕。

技术约束：
- 输出完整的单文件响应式 HTML/CSS/JavaScript 代码（零依赖、无构建步骤）
- 数据持久化使用 localStorage，关闭/刷新页面不丢失数据
- 界面风格：卡通可爱、马卡龙护眼配色、超大按钮，极其适合幼儿手指点击
- 正向激励交互：打卡或答对题目触发满屏彩带/烟花动画，并播放清脆的鼓励音效
- 移动端使用底部大图标导航，桌面端（≥{desktop_breakpoint}px）自动切换为侧边导航
- 确保逻辑严密、无 Bug，交付前对内联脚本做语法校验
```

### 模块 B：多小孩账号系统

**说明**：多子女家庭的核心诉求——每个孩子一套独立数据，避免攀比与混淆。

```text
实现多账号/多小孩切换系统：
- 顶部导航栏提供【小孩头像/名字下拉切换】按钮（如"哥哥·小明"、"妹妹·小红"）
- 支持"新增小孩"与"编辑小孩信息"（名字、年龄/年级、可选个性头像 {avatar_count} 个）
- 数据完全隔离：切换小孩后，其每日计划、已学字库、打卡记录、当前积分、
  看电视时长、已兑换商品及勋章墙自动独立切换，互不影响
- 预置 2 个示例小孩便于开箱体验
```

### 模块 C：幼小衔接知识库

**说明**：内置内容是应用价值核心；数量可按需调整（见参数表）。

```text
内置以下知识库板块：
1. 拼音专区：声母 23 个、韵母 24 个、整体认读音节 16 个，卡片点读发音 + 四声调朗读
2. 识字专区：常用高频字总库（初始优先展示象形字与基础高频字，带拼音、组词及图形提示）
3. 古诗词专区：幼小衔接必背古诗 {poem_count} 首，带注音（可显隐）与标准朗读
4. 英语自然拼读与单词：A-Z 字母拼读 + 主题分类（水果、动物、家庭、数字、颜色、身体），
   含卡片与标准发音（en-US）
5. 数学分合与口算：{math_range} 以内数的分合图解 + 加减法小测试
6. 科普百科：{science_topics} 大知识分类 × 每类 {science_per_topic} 条趣味小百科，
   每条 2~3 句话、5-6 岁能听懂、科学准确，整段语音朗读，分页浏览
7. 经典故事绘本：{story_count} 个睡前经典（童话/寓言/成语故事），
   每个恰好 5 段 + 一句"小道理"，支持整篇朗读、随时停止、展开文字亲子指读，
   结局温和处理、无恐怖血腥内容
8. 生活习惯与自理能力打卡：整理书包/准备衣物、按时睡觉、坐姿握笔、
   独立吃饭不挑食、每天户外运动 30 分钟
```

### 模块 D：智能每日计划与错题复习

**说明**：实现"因材施教 + 小步慢跑"的教育理念。

```text
- 因材施教：每个孩子可独立设定每日目标（如哥哥每天识字 {daily_hanzi_max} 个，
  妹妹每天识字 {daily_hanzi_min} 个）
- 错题自动复习：学习时点击"不熟练"，该卡片自动加入对应小孩次日的复习队列
- 首页展示当日计划完成进度与快捷入口
```

### 模块 E：语音朗读与音色优选

**说明**：Web Speech API 免费但音质因设备而异，需做音色优选策略。

```text
- 所有拼音、汉字、古诗词、英文单词、科普、故事均附带声音图标，点击播放标准发音
- 实现播音级音色自动优选：优先选择 Edge 的 Natural/Online 在线音色
  （如"晓晓/云健"）→ 高质量系统中文语音 → Google 普通话 → 系统默认
- 家长中心提供中文/英文音色手动下拉选择与"试听"按钮，选择结果持久化保存
```

### 模块 F：娱乐/电视时长管理

**说明**：把屏幕时间变成需要"用学习积分购买"的资源，按孩子独立核算。

```text
- 娱乐计时器：看电视/玩游戏计时，消耗各自积分兑换时长
  （{tv_rate_points} 积分兑换 {tv_rate_minutes} 分钟，比例家长可改）
- 超时提醒：达到设定时间（如 {tv_limit} 分钟）自动弹出温馨卡通提醒
  "眼睛累啦，快去户外做游戏吧！"
```

### 模块 G：多元化积分商城

**说明**：非物质奖励优先的设计导向；商品与分类可自由扩容。

```text
积分商城要求（按小孩独立兑换核销）：
- {shop_cat_count} 大分类、每类约 {shop_per_cat} 项，覆盖：
  生活特权（晚睡特权卡/指定晚餐菜单/免家务卡）、家庭陪伴（睡前加讲故事/
  爸妈陪手工/家庭棋牌赛）、户外体验（动物园/骑车/选购绘本）、娱乐时光、
  优选观影（央视推荐/有教育意义的动画与纪录片，如《螺丝钉》《超级宝贝JoJo》
  《中国唱诗班》《航拍中国》《微观小世界》《如果国宝会说话》等）、
  美食甜点、学习装备、成长惊喜
- 兑换后生成带有【小孩名字】的"兑换券"，由家长核销兑现
- 习惯打卡随机触发盲盒，获得 {blindbox_min}~{blindbox_max} 积分
- 商城页提供分类筛选标签栏；老用户数据自动迁移补入新商品，
  但不覆盖家长自定义修改过的商品
```

### 模块 H：家长控制中心

**说明**：对孩子隐蔽、对家长便捷，并内置教育方法论。

```text
- 隐蔽解锁：连续点击顶部 Logo {logo_taps} 次或输入简易密码（默认 {parent_pin}）
- 小孩管理：新增/删除小孩账号，修改名称与头像
- 管理功能：修改每孩学习量、调整积分、补签打卡、编辑商城商品与所需积分
- 预置"家长避坑指南"Tab：①避免多娃攀比 ②小步慢跑原则（每天识字 3-5 个即可）
  ③非物质激励优先 ④灵活补签与积分调整 ⑤屏幕时间选优质内容
- 备份同步：一键导出全部数据为 JSON 文件 / 导入恢复，实现多设备手动迁移
  （隐私优先，不上传任何服务器）
```

### 模块 I：PWA 化与免费部署

**说明**：让 Web 页面变成可安装到手机/平板桌面的"真 App"。

```text
1. 将应用升级为可安装 PWA：
   - 生成 manifest.webmanifest（名称、主题色、standalone 全屏模式）
   - 生成 icon-192.png / icon-512.png 应用图标
   - 编写 sw.js Service Worker 缓存全部静态资源，实现离线可用；
     每次发版必须递增缓存版本号（如 kidsedu-v1 → v2 → v3）
   - index.html 加入 manifest 引用与 apple-touch-icon / 
     apple-mobile-web-app-capable 等 iOS 全屏 meta 标签
2. 部署到 GitHub Pages：
   - git init 并严格通过 .gitignore 排除私有目录（如 .workbuddy/）
   - 创建公开仓库并推送 main 分支（PAT 只需 repo 权限）
   - 添加 .nojekyll 文件跳过 Jekyll，避免静态站构建卡死
   - 通过 API 开启 Pages（main / 根目录），轮询 HTTP 200 确认上线
3. 生成站点二维码 qrcode.png 方便家人扫码安装
```

### 模块 J：大规模内容扩容（并行子代理）

**说明**：单次生成上千条内容易超限、易出错；应拆分给并行子代理，并用机器校验兜底。

```text
需要生成 {science_total} 条科普 + {story_count} 个故事时：
1. 按知识分类拆分为多个数据分件（每件约 200 条），并行派发给多个子代理
2. 每个子代理的提示词必须包含：
   - 严格的输出格式模板（window.XXX=[...] 单条 JS 语句）
   - 硬性数量要求（"恰好 N 条，不得少"）
   - 转义安全规则：字符串只用 ASCII 单引号定界，文本内部禁止出现
     单引号/反斜杠/英文双引号，标点全用中文，不得出现 </script 字样
   - 自校验命令：node --check 语法校验 + node -e 加载统计条数
3. 主流程合并分件为 data-*.js 外部文件，再次整体校验
   （条数、格式、重复标题、非法字符），通过后接入页面并更新 sw.js 缓存清单
```

---

## 3. 使用示例

### 示例 1：一次性完整生成

将模块 A–H 全文拼接为一条消息发给 AI 助手，替换所有 `{占位符}`：

> "请帮我开发一个专为幼小衔接（5-6 岁）及多子女家庭设计的响应式 Web 工作台……（模块 A）……实现多账号切换……（模块 B）……内置以下知识库……（模块 C）……"

**预期产出**：单文件 `index.html`（约 2000+ 行），打开即用。

### 示例 2：增量添加新板块

已有应用后，只发送一条聚焦的增量需求：

> "在知识库中加入『科普百科』与『经典故事绘本』板块（语音播放），并在积分商城中新增『央视推荐/有教育意义的儿童动画片与纪录片』（如《螺丝钉》《航拍中国》《微观小世界》等）的兑换选项。要求老用户数据自动迁移、不覆盖家长自定义商品。"

**要点**：增量需求要显式声明**数据迁移规则**，否则老用户 localStorage 可能不出现新内容。

### 示例 3：部署到 GitHub Pages

> "帮我部署到 GitHub Pages，我已有 GitHub 账号。"
> （AI 询问认证方式后，提供仅 `repo` 权限、短有效期的 classic PAT）

**要点**：
- token 用完立即到 GitHub Revoke
- 若 Pages 构建长时间卡在 building，添加 `.nojekyll` 后重推即可解决

### 示例 4：内容批量扩容

> "商城兑换内容丰富一些，多一些分类，每个分类 10 条左右。语音使用播音员音质，故事绘本增加到 50 个，科普按知识分类整 1000 个。"

**预期行为**：AI 应按模块 J 拆分并行生成 + 机器校验，而不是单次硬写 1000 条。

---

## 4. 参数说明

| 参数 | 含义 | 推荐取值 | 本仓库取值 |
|---|---|---|---|
| `{age_range}` | 目标年龄段 | 5-6 岁（大班）；可改 3-4 / 6-7 | 5-6 岁 |
| `{desktop_breakpoint}` | 桌面侧边栏断点 | 900–1024 px | 920 px |
| `{avatar_count}` | 可选头像数量 | 8–16 | 12 |
| `{poem_count}` | 内置古诗数量 | 20–100 首 | 21 首 |
| `{math_range}` | 口算数值范围 | 5 / 10 / 20 以内 | 5、10、20 三档 |
| `{science_topics}` | 科普知识分类数 | 5–12 | 10 |
| `{science_per_topic}` | 每分类科普条数 | 20–100 | 100 |
| `{science_total}` | 科普总条数 | 100–1000 | 1000 |
| `{story_count}` | 故事绘本数量 | 8–50 | 50 |
| `{daily_hanzi_min}`/`{daily_hanzi_max}` | 每日识字目标区间 | 2–5 个/天（大班不宜超过 5） | 默认 3，可 1–20 |
| `{tv_rate_points}` : `{tv_rate_minutes}` | 积分↔娱乐时长比例 | 20 分 : 15 分钟左右 | 20 : 15（可改） |
| `{tv_limit}` | 单次娱乐上限提醒 | 20–40 分钟 | 30 分钟 |
| `{shop_cat_count}` | 商城分类数 | 4–8 | 8 |
| `{shop_per_cat}` | 每分类商品数 | 6–10 | 约 10 |
| `{blindbox_min}`~`{blindbox_max}` | 盲盒积分区间 | 5~15 | 5~15 |
| `{logo_taps}` | 家长模式连点次数 | 5–7 | 5 |
| `{parent_pin}` | 家长 PIN 码 | 4 位数字，部署后务必修改 | 8888 |

---

## 5. 注意事项与最佳实践

1. **PWA 必须走 https**：本地 `file://` 打开只能浏览，无法"添加到主屏幕"和离线缓存。
2. **每次发版递增 Service Worker 缓存版本号**，否则已安装用户拿不到新内容。
3. **数据隐私优先**：所有数据仅存设备本地 localStorage；跨设备用导出/导入 JSON 备份，不引入后端。
4. **token 安全**：部署用 PAT 只勾 `repo` 权限、设短有效期、用完即撤销；绝不将 token 写入任何文件或仓库。
5. **私有目录隔离**：AI 工作目录（如 `.workbuddy/`）必须加入 `.gitignore`，避免推到公开仓库。
6. **子代理产出必须机器校验**：`node --check` 语法 + 脚本统计条数/格式/重复，人眼不可靠。
7. **教育原则**：小步慢跑、避免多娃横向对比、非物质奖励优先——这些已内置在避坑指南中，二次开发时建议保留。

---

*本文档整理自真实 AI 辅助开发对话（2026-07），随仓库以 MIT 协议开源，欢迎复用与改进。*
